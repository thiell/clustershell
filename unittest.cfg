[nose2]
start-dir = tests
verbosity = 2
plugins = nose2.plugins.coverage

[discovery]
# Match your naming scheme: SomethingTest.py
pattern = *Test.py

[coverage]
always-on = True
coverage = ClusterShell
coverage-report = term
# while migrating, avoid hard-failing on “no data” edge cases
fail-under = 0
