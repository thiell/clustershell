[nose2]
start-dir = tests
top-level-directory = .
verbosity = 2
plugins = nose2.plugins.coverage

[discovery]
pattern = *Test.py

[coverage]
always-on = True
coverage = ClusterShell
coverage-report = term
fail-under = 0
